CREATE TABLE ACCOUNTS(
id INT PRIMARY KEY AUTO_INCREMENT,
name VARCHAR(255) NOT NULL);

CREATE TABLE BILLS(
id INT PRIMARY KEY AUTO_INCREMENT,
bill_number VARCHAR(255) NOT NULL,
amount NUMERIC(19,2) NOT NULL,
account_id INT NOT NULL);
CREATE TABLE CARDS(
id INT PRIMARY KEY AUTO_INCREMENT,
card_number VARCHAR(255) NOT NULL,
bill_number VARCHAR(255) NOT NULL);

CREATE TABLE DEPOSITS(
id INT PRIMARY KEY AUTO_INCREMENT,
bill_number VARCHAR(255) NOT NULL,
amount NUMERIC(19,2) NOT NULL);

CREATE TABLE COUNTERPARTY(
id INT PRIMARY KEY AUTO_INCREMENT,
employer_id VARCHAR(255) NOT NULL,
counterparty_id VARCHAR(255) NOT NULL);

CREATE TABLE TRANSFERS(
id INT PRIMARY KEY AUTO_INCREMENT,
sender_bill VARCHAR(255) NOT NULL,
recipient_bill VARCHAR(255) NOT NULL,
amount NUMERIC(19,2) NOT NULL);

CREATE TABLE CONFIRMATION(
id INT PRIMARY KEY AUTO_INCREMENT,
entity_name VARCHAR(255) NOT NULL,
operation VARCHAR(255) NOT NULL,
info VARCHAR(255) NOT NULL,
confirmation_status VARCHAR(255) DEFAULT 'NOT CONFIRM');

INSERT INTO ACCOUNTS(NAME)
VALUES ('Павел'),
       ('Максим'),
       ('Глеб');

INSERT INTO BILLS(BILL_NUMBER, AMOUNT, ACCOUNT_ID)
VALUES ('11111', 50000, 1),
       ('22222', 100000, 2),
       ('33333', 10000, 3);

INSERT INTO CARDS(CARD_NUMBER, BILL_NUMBER)
VALUES ('1111222233334444', '11111'),
       ('1234123412341234', '11111'),
       ('1122223333444455', '22222'),
       ('1111111111111111', '33333');